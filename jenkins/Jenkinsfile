pipeline {
    agent any

    stages {
        stage('Check Git') {
            steps {
                sh 'git --version'
                sh 'ls -la'
            }
        }
        stage('Check Docker') {
            steps {
                sh 'docker --version'
                sh 'docker ps'
            }
        }
    }
}
